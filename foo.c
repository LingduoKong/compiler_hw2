#include <stdio.h>
#include <string.h>
#define min(X, Y)  ((X) < (Y) ? (X) : (Y))
#define max(A, B)  ((A) < (B) ? (B) : (A))
main()
{
/* comment 1 */
// abc
   String str = "abc";
   String s = "12345";
   min (3, 4);
   max(2, 4);
   max (3, 9);
   return min (1,2);
}
